export function renderCapabilities(caps, heading) {
  return `<section class="capabilities-section section-space" id="capabilities" aria-labelledby="capabilities-title">${heading("FROM IDEA TO SOMETHING USEFUL", "Good ideas deserve<br><em>thoughtful execution.</em>", "capabilities-title")}<div class="capability-intro"><span aria-hidden="true">✳</span><p>I connect development with testing, so the things I build feel good to use and hold up behind the scenes.</p></div><div class="capabilities" data-capabilities><p class="capability-label">5 ways I put code to work</p>${caps.map(([title, desc, , detail], i) => `<details class="capability" name="capabilities"${i === 0 ? " open" : ""}><summary><span class="capability-number" aria-hidden="true"><svg viewBox="0 0 40 40"><circle class="capability-track" cx="20" cy="20" r="17"/><circle class="capability-progress" cx="20" cy="20" r="17" pathLength="100"/></svg><span>${i + 1}</span></span><span><strong>${title}</strong><span class="capability-description">${desc}</span></span><span class="capability-plus" aria-hidden="true">+</span></summary><div class="capability-detail"><p>${detail}</p></div></details>`).join("")}</div></section>`;
}

export function renderHomeAbout(heading, quickAsk, link) {
  return `<section class="about-section section-space" id="about" aria-labelledby="about-title">${heading("THE HUMAN BEHIND THE CODE", "A little bit about <em>me.</em>", "about-title")}<div class="wallet" aria-label="A few things about Baivab">
  <a class="wallet-card wallet-purple" id="wallet-work" href="/work/markdown-viewer"><small>On my workbench</small><span class="wallet-icon" aria-hidden="true">M↓</span><strong>Markdown Viewer</strong><span>Always another idea.</span></a>
  <a class="wallet-card wallet-yellow" id="wallet-me" href="/about"><small>Hello, I’m</small><img src="/assets/profile/baivab-480.webp" width="480" height="517" alt="" loading="lazy" decoding="async" draggable="false"><strong>Baivab Sarkar</strong><span>Curious by default.</span></a>
  <a class="wallet-card wallet-green" id="wallet-study" href="/about#experience"><small>My foundation</small><span class="wallet-icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><path d="m2 9 10-5 10 5-10 5Zm4 3v5c4 3 8 3 12 0v-5M22 9v7"/></svg></span><strong>Computer science</strong><span>JIS · Class of 2025</span></a>
  <a class="wallet-card wallet-pink" id="wallet-notes" href="/blog"><small>Learning in public</small><span class="wallet-icon" aria-hidden="true">Aa</span><strong>Build & write</strong><span>Notes from the process.</span></a>
  <a class="wallet-card wallet-blue" id="wallet-code" href="https://github.com/ThisIs-Developer"><small>Find me in the open</small><span class="wallet-icon" aria-hidden="true">{ }</span><strong>ThisIs-Developer</strong><span>West Bengal, India</span></a>
  <div class="wallet-switcher" role="group" aria-label="Choose a wallet card" hidden>${["Work", "Me", "Study", "Notes", "Code"].map(label => `<button type="button" data-wallet-select="${label.toLowerCase()}" aria-controls="wallet-${label.toLowerCase()}" aria-pressed="${label === "Me"}">${label}</button>`).join("")}</div>
  </div>${quickAsk()}<div class="section-bottom">${link("/about", "A little more about me", "button")}</div></section>`;
}
